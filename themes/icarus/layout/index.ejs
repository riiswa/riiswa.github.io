<% if (page["__index"]) { %>

<% } %>

<% page.posts.each(function(post){ %>
    <%- _partial('common/preview', { post, index: true }) %>
<% }); %>
<% if (page.total > 1) { %>
    <%- _partial('common/paginator') %>
<% } %>